# To Do

- Mobile ergonomics
  - QR code generation
  - Automatic gateway selection
- Drops
  - DNS TXT
- Make `join` command able to be run from the client side
  - I think it currently has to be run on the admin node so it can see the
    required keys, etc.
